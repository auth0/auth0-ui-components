/** Default interval (ms) used to refresh the relative "last updated" label. */
export const DEFAULT_REFRESH_INDICATOR_TICK_MS = 30_000;

/**
 * Member access levels.
 */
export const MEMBER_ACCESS_LEVELS = ['limited', 'full'] as const;
