/**
 * Member management related constants for the My Organization section.
 * @internal
 */

export const MEMBER_MANAGEMENT_PAGE_SIZE_OPTIONS = [10, 25, 50];

/**
 * Shared dialog content styling used across member management modals.
 * Mirrors the elevated layer surface from the design system.
 * @internal
 */
export const MEMBER_MANAGEMENT_MODAL_CLASSNAME =
  'inline-flex flex-col items-start gap-6 rounded-lg bg-background p-10 shadow-[0_16px_16px_0_rgba(0,0,0,0.10)]';
